To install tracker you must do the next steps:

1) go to apm and install the tracker package in the common way
2) cd openacs-4/packages/
3) cat tracker/www/doc/acs-tcl.patch | patch
4) reboot the web server

